apiVersion: v1
kind: Service
metadata:
  name: forya-be
  labels:
    app: forya-be
spec:
  type: ClusterIP
  selector:
    app: forya-be
  ports:
    - name: http
      port: 80
      targetPort: 8080
